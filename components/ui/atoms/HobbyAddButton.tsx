const HobbyAddButton = ({ onChange }) => {
  return (
    <button className="" onChange={onChange}>
      <p className="text-purple-400 text-xs font-bold">趣味追加</p>
    </button>
  );
};

export default HobbyAddButton;
